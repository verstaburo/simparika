/* eslint-disable */
// https://github.com/jshjohnson/Choices
import Choices from 'choices.js';

const $ = window.$;

export default function citySelect () {
  /*
  const selectClass = '.js-city-select';

  if (!$(selectClass).length) return;

  const select = new Choices(selectClass, {
    searchEnabled: true,
    searchChoices: true,
    itemSelectText: '',
    classNames: {
      containerOuter: 'choices city-select',
    },
    noResultsText: 'Город не найден',
  });*/
};

/* eslint-enable */
